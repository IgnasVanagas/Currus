import { ReactNode } from "react";
type AnimatedSectionProps = {
    id: string;
    children: ReactNode;
    className?: string;
};
export declare const AnimatedSection: ({ id, children, className }: AnimatedSectionProps) => import("react/jsx-runtime").JSX.Element;
export {};
